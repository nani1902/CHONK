-r requirements.txt
pytest>=8.0
# Builds the disposable signing certificate for the signature-bearing fixture.
cryptography>=42.0
# Validates results against the generated JSON Schemas (tests/contracts).
jsonschema>=4.18
